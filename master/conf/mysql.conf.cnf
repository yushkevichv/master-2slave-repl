[mysqld]

server-id = 1
log_bin = /var/log/mysql/mysql-bin.log
log-slave-updates
enforce-gtid-consistency

binlog_format = ROW
binlog_do_db = test_repl_db

skip-host-cache
skip-name-resolve
gtid_mode = ON

